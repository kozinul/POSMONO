# Leave MONGO_URI unset to use mongodb-memory-server (cached binary auto-detected).
# Set MONGO_URI to point at an external MongoDB (e.g. docker-compose.test.yml or CI service).
# MONGO_URI=mongodb://localhost:27027/posmono_test
JWT_SECRET=test-secret-for-testing-only
NODE_ENV=test
